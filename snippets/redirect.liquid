{% if 
    template == 'page.qurify-offer' 
    or template == 'page.why-qurify'
    or template == 'page.sunil-free-water-report'
    or template == 'page.sunil-bf-sales'
    or template == 'page.bf-sales-v2'
    or template == 'page.exclusive-holiday-bundle-deal'
    or template == 'page.exclusive-holiday-bundle-deal-uk'
%}
    <script src="{{ '__app-redirect.js' | asset_url }}"></script>
{% endif %}

{% if
    product.handle == 'q-urify-water-filter' 
    or product.handle == 'confidence-booster-bundle'
    or product.handle == 'youthify-or-glow-set'
    or product.handle == 'skin-ssentials-set-doctorly-choice'
    or product.handle == 'q-rify-replacement-water-filter'
    or product.handle == 'confidence-booster-bundle'
    or product.handle == 'faucet-filter'
    or product.handle == 'breakout-control-pro-1'
    or product.handle == 'breakout-control-starter'
    or product.handle == 'filter-family-bundle'
    or product.handle == 'line-refine-bundle-advanced-1'
    or product.handle == 'shower-filter'
%}
    <script src="{{ '__app-redirect.js' | asset_url }}"></script>
{% endif %}

{% if 
    template == 'collection'
    or template == 'collection.acne-breakouts'
    or template == 'collection.fine-lines-wrinkles'
%}
    <script src="{{ '__app-redirect.js' | asset_url }}"></script>
{% endif %}